{
  "mcpServers": {
    "devdigest": {
      "type": "stdio",
      "command": "pnpm",
      "args": ["--dir", "${workspaceFolder}/mcp", "exec", "tsx", "src/index.ts"],
      "env": {
        "DEVDIGEST_API_BASE": "http://localhost:3001"
      }
    }
  }
}
