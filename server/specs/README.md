# server/specs

Feature/behavior specs for `@devdigest/api` — the expected behavior of a
module before/while it's implemented, not the implementation itself (that's
what the code and `../docs/` are for).

Nothing here yet — add one `.md` per feature as it's speced out. Link new
specs from [`../CLAUDE.md`](../CLAUDE.md) if an agent needs to follow them
while implementing.
