# server/docs

Architecture notes and design decisions for `@devdigest/api` that don't belong
in the package README (which covers the request/DI flow and public routes).

Nothing here yet — add a `.md` per decision/subsystem as they come up (e.g.
`repo-intel-indexing.md`, `review-run-lifecycle.md`). Link new docs from
[`../README.md`](../README.md) and from [`../CLAUDE.md`](../CLAUDE.md)
if they affect conventions an agent needs.
