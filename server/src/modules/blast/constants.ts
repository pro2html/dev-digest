/**
 * Blast module constants.
 */
/** Max prior PRs returned on the blast map. */
export const MAX_PRIOR_PRS = 8;
/** Cap overlap paths shown per prior PR (full count still in overlap_count). */
export const MAX_OVERLAP_PATHS = 12;
