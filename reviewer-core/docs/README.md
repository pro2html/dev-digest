# reviewer-core/docs

Architecture notes and design decisions for `@devdigest/reviewer-core` that
don't belong in the package README (which covers the prompt→LLM→grounding
pipeline and public API).

Nothing here yet — add a `.md` per decision/subsystem as they come up (e.g.
`grounding-algorithm.md`, `structured-output-repair.md`). Link new docs from
[`../README.md`](../README.md) and from [`../AGENTS.md`](../AGENTS.md)
if they affect conventions an agent needs.
